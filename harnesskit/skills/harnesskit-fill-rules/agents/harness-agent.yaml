interface:
  display_name: "生成 Rule Claims"
  short_description: "生成或选择实践规则的 Markdown Claims"
  default_prompt: "使用 $harnesskit-fill-rules 在 Bootstrap 或选择性 Adopt 中维护 root/part Rule Claims，并通过 deterministic tooling 建立 provenance。"
