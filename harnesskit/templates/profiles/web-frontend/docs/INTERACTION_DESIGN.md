# [PROJECT_NAME] 交互设计

本文件记录 Web 前端关键用户流程、交互状态、反馈与恢复规则，帮助贡献者修改用户可见行为前理解完整体验。视觉令牌与组件外观属于 [DESIGN_SYSTEM.md](DESIGN_SYSTEM.md)，产品原则属于 `docs/rules/PRODUCT_SENSE.md`，代码运行链路属于 [ARCHITECTURE.md](ARCHITECTURE.md)。

<!-- harnesskit:todo-checklist:start -->
补全本文件前请确认：

- 从真实页面、路由、组件状态、产品文档、测试和可访问性实现中核对每条结论。
- 同时覆盖正常路径、等待、空状态、失败、取消和恢复；不要只描述理想 happy path。
- 区分当前已实现行为与团队确认的未来做法，不把常识或模板建议写成项目事实。
- 只记录跨文件或高影响的交互契约，不复制逐页面文案、视觉令牌或源码细节。
<!-- harnesskit:todo-checklist:end -->

## 关键任务与用户流程

记录用户完成核心任务时的入口、关键步骤、退出路径和跨页面连续性：

## 交互状态与转换

记录会影响实现判断的状态、触发条件、转换、并发限制和可逆性：

## 反馈、错误与恢复

记录等待反馈、成功确认、错误呈现、重试、撤销、离线或中断恢复等行为：

## 输入与操作约定

记录表单提交、校验、键盘操作、指针操作、快捷方式和防重复操作等统一约定：

## 响应式与跨设备行为

记录不同视口、输入方式或设备能力下会改变流程或控件行为的边界：

## 可访问性

记录焦点顺序、语义通知、键盘可达性、替代交互和减少动态效果等交互要求：

## 更新规则

当关键流程、交互状态、反馈恢复、输入方式或可访问性行为发生变化时，同步更新本文件和对应测试或产品文档。
