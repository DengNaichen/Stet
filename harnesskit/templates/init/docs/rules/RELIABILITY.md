# Reliability Rules

本文件记录当前仓库改动风险较高的区域和可靠性硬约束。它回答"在这个项目里，改哪些东西需要格外小心"，不重复通用可靠性建议。

<!-- harnesskit:todo-checklist:start -->
补全本文件前请确认：
- 从真实测试、hook、CI、release、兼容性、生成输出、持久数据和运维边界中提炼风险区域。
- 按影响范围和失败代价排序；不要把所有目录都列成高风险。
- 区分 command、validation 和 runner；不要把未配置检查写成 gate。
- 验证项、命令和检查状态归 [VALIDATION.md](../VALIDATION.md)，本文件只用链接引用、不复制。
- 路径职责和放置规则进入 [ARCHITECTURE.md](../ARCHITECTURE.md)。
- 只写已确认内容；当前没有可记录事实时可保留对应小节为空。
<!-- harnesskit:todo-checklist:end -->

## 高风险改动区域

按风险从高到低排列：

| 区域 | 影响范围 | 为什么风险高 |
|------|----------|-------------|

## 高风险确认边界

| 区域 | 改动前问题 | 最低验证 | 需要确认时机 |
|---|---|---|---|

## 改动前检查矩阵

| 场景 | 容易漏掉 | 必须同步核对 |
|------|----------|--------------|

## 验证覆盖

完整的验证项、命令、runner 和状态语义见 [VALIDATION.md](../VALIDATION.md)，本文件不复制命令或检查状态。本节只记录可靠性视角的补充：

- 如发现可靠性风险缺少对应自动化检查，在 [VALIDATION.md](../VALIDATION.md) 补登记，不要在本文件维护第二份清单。

## 硬约束

以下规则必须始终成立，违反即为错误：
