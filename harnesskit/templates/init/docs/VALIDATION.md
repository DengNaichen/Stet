# [PROJECT_NAME] 验证

本文件只记录当前仓库自身已确认的验证项、真实执行入口、自动化绑定和结果报告要求。硬约束分布在各实践文件和 [ARCHITECTURE.md](ARCHITECTURE.md) 的“硬约束”段，操作入口放在 [AGENTS.md](../AGENTS.md)。

不要假设仓库使用特定 VCS、runner、hook、CI 或平台 gate。只有从目标仓库已有配置、脚本、清单、workflow、测试或团队确认中核对过的内容，才能写成已配置 validation。

<!-- harnesskit:todo-checklist:start -->
补全本文件前请确认：

- 每个验证项都写明真实命令或入口、适用范围和证据路径。
- 未配置、未运行、无法运行与运行失败必须准确区分，不把候选命令写成已有检查。
- Runner、hook、CI 和 platform gate 只记录目标仓库真实存在的绑定。
- 只记录会改变 agent 验证与交付判断的内容，不复制测试目录或 workflow 的完整清单。
<!-- harnesskit:todo-checklist:end -->

## 验证项

| 项目 | 状态 | 命令 / 入口 | 适用范围 | 证据 |
| --- | --- | --- | --- | --- |

## 执行入口与绑定

记录目标仓库已确认的本地 runner、hook、CI 或平台绑定；没有统一入口时明确写出各项独立命令，不发明聚合 runner。

## 结果报告

- 已运行项：报告实际命令或入口及结果。
- 未运行或无法运行项：报告项目、原因和影响。
- 失败项：保留真实失败状态，不把局部通过概括为完整验证通过。
