# Cursor adapter

Repository-local Cursor lifecycle configuration. Agent routing and project
knowledge live in [`AGENTS.md`](../AGENTS.md), [`docs/`](../docs/index.md), and
[`reference/`](../reference/README.md) — not here.

## Files

- `worktrees.json` — Cursor worktree setup hook target (see `scripts/setup-worktree`).

Hooks and MCP config may be added later; keep tool-specific wiring in this
directory only.
